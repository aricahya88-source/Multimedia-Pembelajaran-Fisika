import Link from 'next/link'; import AppShell from '@/components/AppShell'; import AuthGate from '@/components/AuthGate'; import GlassCard from '@/components/GlassCard'; import RichHtml from '@/components/RichHtml'; import { GUIDES } from '@/lib/guideContent'; import { ArrowLeft } from 'lucide-react';
export default async function GuidePage({params}:{params:Promise<{code:string}>}){const{code}=await params;const d=GUIDES[code];return <AuthGate><AppShell title="Panduan & Rubrik"><Link href="/guides" className="button soft compact"><ArrowLeft/>Kembali</Link>{!d?<div className="error-box">Panduan tidak ditemukan.</div>:<GlassCard className="material-card"><span className="eyebrow">PANDUAN AKTIVITAS</span><h2>{d.title}</h2><p className="muted">{d.subtitle}</p><RichHtml html={d.html} className="rich-html material-html"/></GlassCard>}</AppShell></AuthGate>}
