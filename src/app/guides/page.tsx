import Link from 'next/link'; import AppShell from '@/components/AppShell'; import AuthGate from '@/components/AuthGate'; import { GUIDES } from '@/lib/guideContent'; import { BookOpenCheck,ArrowRight } from 'lucide-react';
export default function GuidesPage(){return <AuthGate><AppShell title="Panduan & Rubrik"><div className="section-title"><div><span className="eyebrow">PANDUAN AKTIVITAS</span><h2>Petunjuk Semester</h2><p className="muted">Baca panduan sebelum menyusun rencana atau submission.</p></div></div><div className="project-grid">{Object.entries(GUIDES).map(([code,g])=><Link href={`/guides/${code}`} className="glass-card project-tile" key={code}><div className="icon-bubble teal"><BookOpenCheck/></div><div className="grow"><h3>{g.title}</h3><p>{g.subtitle}</p></div><ArrowRight/></Link>)}</div></AppShell></AuthGate>}
