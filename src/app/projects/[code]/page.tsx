'use client';
import { use } from 'react'; import Link from 'next/link'; import AppShell from '@/components/AppShell'; import AuthGate from '@/components/AuthGate'; import ProjectPlanForm from '@/components/ProjectPlanForm'; import ProjectFinalReport from '@/components/ProjectFinalReport'; import { projectByCode } from '@/lib/projectThemes'; import { ArrowLeft } from 'lucide-react';
export default function ProjectPage({params}:{params:Promise<{code:string}>}){const{code}=use(params);const key=code.toUpperCase(),def=projectByCode(key);return <AuthGate><AppShell title={def?.name||'Proyek'}><Link href="/projects" className="button soft compact"><ArrowLeft/>Kembali</Link><div className="stack"><ProjectPlanForm code={key}/><ProjectFinalReport code={key}/></div></AppShell></AuthGate>}
